import { FocuserDirective } from './focuser.directive';

describe('FocuserDirective', () => {
  it('should create an instance', () => {
    const directive = new FocuserDirective();
    expect(directive).toBeTruthy();
  });
});
