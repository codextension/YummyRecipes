import { SelectTextDirective } from './select-text.directive';

describe('SelectTextDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectTextDirective();
    expect(directive).toBeTruthy();
  });
});
